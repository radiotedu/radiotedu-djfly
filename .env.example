# Used only by the Python preprocessing commands. Never expose this to a browser.
NEUPRINT_TOKEN=
DJFLY_GRAPH_SOURCE=REAL_MALECNS
# Absolute path to a reviewed, generated artifact on the event server.
# Optional override; otherwise generated/runtime-manifest.json selects and verifies the real artifact.
DJFLY_GRAPH_PATH=
DJFLY_GRAPH_SHA256=
